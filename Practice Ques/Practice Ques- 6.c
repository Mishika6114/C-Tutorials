// Ques 6- Write a program to check if a number is even o odd.
// even -> 1
// odd --> 0

#include<stdio.h>
#include<math.h>

int.main () {
    int x;
    printf("enter a number : ");
    scanf("%d", x);
    printf("%d", x % 2 == 0);

    return 0;
}